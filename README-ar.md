# ⛏️ Mining Farm Project - نظام إدارة مزرعة التعدين

نظام إدارة مزرعة تعدين العملات المشفرة التلقائي مع دعم متعدد العملات، مراقبة في الوقت الفعلي، و API مركزي باستخدام Flask. يدعم Monero، Epic Cash، QRL، والمزيد.

## ✨ المميزات

- ⛏️ **تعدين متعدد العملات**: دعم Monero، Epic Cash، QRL، والمزيد
- 📊 **مراقبة في الوقت الفعلي**: تتبع أداء أجهزة التعدين مباشرة
- 🤖 **بوت تيليجرام**: إدارة ومراقبة من خلال Telegram
- 🖥️ **خادم مركزي**: API مركزي لإدارة جميع أجهزة التعدين
- 📈 **تقارير مفصلة**: إحصائيات وأداء مفصل لكل جهاز
- 🔄 **إدارة تلقائية**: إدارة تلقائية لأجهزة التعدين

## 🚀 البدء السريع

### المتطلبات

- Python 3.8+
- MySQL
- أجهزة تعدين (Miners)
- حساب Telegram Bot Token

### التثبيت

1. استنسخ المستودع:
```bash
git clone https://github.com/3bkader-gpt/mining-farm-project.git
cd mining-farm-project
```

2. ثبت المتطلبات:
```bash
pip install -r requirements.txt
```

3. قم بإعداد قاعدة البيانات:
```bash
# أنشئ قاعدة بيانات MySQL
mysql -u root -p
CREATE DATABASE mining_farm;
```

4. قم بإعداد ملف `.env`:
```bash
cp .env.example .env
# قم بتعديل ملف .env بإضافة بياناتك
```

5. قم بتشغيل الخادم المركزي:
```bash
python central_server.py
```

6. قم بتشغيل البوت:
```bash
python telegram_bot.py
```

## 📁 هيكل المشروع

```
mining-farm-project/
├── central_server.py   # الخادم المركزي (API)
├── telegram_bot.py     # بوت Telegram
├── worker_script.py    # سكريبت العامل (على أجهزة التعدين)
├── .env.example        # مثال ملف البيئة
└── requirements.txt    # المتطلبات
```

## ⚙️ الإعدادات

### إعداد قاعدة البيانات

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=mining_farm
```

### إعداد Telegram Bot

1. تحدث مع [@BotFather](https://t.me/BotFather)
2. أنشئ بوت جديد واحصل على Token
3. أضف Token في ملف `.env`:
```
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### إعداد أجهزة التعدين

1. ثبت `worker_script.py` على كل جهاز تعدين
2. قم بتعديل الإعدادات في السكريبت
3. شغل السكريبت على كل جهاز

## 📝 الاستخدام

### الخادم المركزي

الخادم المركزي يوفر API RESTful لإدارة أجهزة التعدين:

- `GET /api/workers` - قائمة جميع الأجهزة
- `GET /api/workers/{id}` - معلومات جهاز معين
- `GET /api/stats` - إحصائيات عامة

### بوت Telegram

استخدم البوت للتحكم والمراقبة:

- `/status` - حالة جميع الأجهزة
- `/worker {id}` - معلومات جهاز معين
- `/stats` - إحصائيات عامة
- `/start_mining` - بدء التعدين
- `/stop_mining` - إيقاف التعدين

## 🛠️ التقنيات المستخدمة

- **Python**: لغة البرمجة الأساسية
- **Flask**: إطار عمل الويب للـ API
- **MySQL**: قاعدة البيانات
- **python-telegram-bot**: بوت Telegram
- **XMRig**: برنامج التعدين (لـ Monero)
- **Requests**: للاتصال بالـ API

## 🔧 العملات المدعومة

- **Monero (XMR)**: تعدين CPU/GPU
- **Epic Cash**: تعدين CPU
- **QRL (Quantum Resistant Ledger)**: تعدين CPU
- والمزيد...

## 📊 المميزات المتقدمة

- **مراقبة في الوقت الفعلي**: تحديثات فورية لحالة الأجهزة
- **إدارة تلقائية**: إدارة تلقائية للأجهزة
- **تقارير مفصلة**: إحصائيات وأداء مفصل
- **تنبيهات**: إشعارات عند حدوث مشاكل

## 🔒 الأمان

- تأكد من حماية قاعدة البيانات
- استخدم HTTPS للـ API
- قم بتأمين أجهزة التعدين
- لا تشارك مفاتيح API

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## ⚠️ تحذير

التعدين يتطلب موارد كبيرة وقد يستهلك كهرباء كثيرة. تأكد من:
- فهم تكاليف الكهرباء
- استخدام أجهزة مناسبة
- مراقبة درجات الحرارة
- الالتزام بالقوانين المحلية

## 🤝 المساهمة

نرحب بالمساهمات! يرجى فتح Issue أو Pull Request.

## 📧 التواصل

للاستفسارات والدعم، يرجى فتح Issue في المستودع.

---

**ملاحظة**: هذا المشروع للاستخدام التعليمي. تأكد من فهم مخاطر تعدين العملات المشفرة.